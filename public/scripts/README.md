# Narration scripts

Store UTF-8 narration text here and reference it with `/scripts/name.txt` in a Worldlayer job. Phase 5's `mock` provider produces diagnostic tones per word so the local pipeline can be tested without a TTS service. It does not generate spoken narration.
