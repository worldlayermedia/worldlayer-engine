# Local narration files

Place prerecorded WAV or MP3 narration here and reference it as `/audio/name.wav` or `/audio/name.mp3` in a Worldlayer job. Audio files are ignored by Git so large recordings stay local. The narrated Toronto development job uses `/audio/worldlayer-toronto-dev.wav`.
